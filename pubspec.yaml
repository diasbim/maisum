name: loyalty_app
description: MaisUm — offline-first loyalty for small businesses
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  hooks_riverpod: ^2.6.1
  flutter_hooks: ^0.20.5

  # Navigation
  go_router: ^14.6.3

  # Networking
  dio: ^5.7.0

  # Database
  sqflite: ^2.3.3
  path: ^1.9.1

  # Code generation models
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

  # Secure storage
  flutter_secure_storage: ^9.2.4

  # Connectivity
  connectivity_plus: ^6.1.4

  # Utilities
  uuid: ^4.5.1
  intl: ^0.19.0
  logger: ^2.5.0
  url_launcher: ^6.3.1
  google_fonts: ^6.2.1
  pinput: ^5.0.0

  # Firebase
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.15
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  sqflite_common_ffi: ^2.3.0
  sqlite3: ">=2.3.2 <3.0.0"
  firebase_auth_mocks: ^0.14.1
  fake_cloud_firestore: ^3.0.4

flutter:
  uses-material-design: true
  assets:
    - assets/images/
